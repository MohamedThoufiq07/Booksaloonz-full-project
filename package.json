{
    "name": "booksaloonz-root",
    "version": "1.0.0",
    "description": "Root for BookSaloonz Project",
    "scripts": {
        "install-all": "npm install && npm install --prefix \"booksaloonz backend\" && npm install --prefix \"booksaloons frontend\"",
        "backend": "npm run dev --prefix \"booksaloonz backend\"",
        "frontend": "npm run dev --prefix \"booksaloons frontend\"",
        "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.1"
    }
}